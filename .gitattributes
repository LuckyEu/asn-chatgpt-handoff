# Store public markdown/captions verbatim (LF bytes) — no EOL conversion
latest.md -text
artifacts/bfm-video-current/README.md -text
artifacts/bfm-video-current/*.vtt -text
artifacts/bfm-video-current/*.srt -text

# JSON keeps normalized LF
*.json text eol=lf
